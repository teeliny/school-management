NEXT_PUBLIC_API_URL="http://localhost:3001"
REDIS_URL="redis://localhost:6379"

# Public base URL this deployment is served from — used for canonical links,
# Open Graph tags, and the sitemap/robots routes. Set to the real production
# domain when deploying (e.g. "https://mercylag.sch-portals.com").
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
