/** Public base URL this deployment is served from — used for canonical links,
 * Open Graph tags, and the sitemap/robots routes. */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000";
