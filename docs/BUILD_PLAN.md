# Build Plan: School Management System

**Status:** Draft v1.0
**Companion to:** [PRD.md](./PRD.md) (what/why) and [ARCHITECTURE.md](./ARCHITECTURE.md) (how/structure) — this document is the *sequencing*: build order, cross-phase dependencies, and a concrete "done" definition per phase, so implementation can start without re-deriving the order from scratch.

---

## 1. Build Order Principles

- **This is a single-tenant application.** One deployment, one database, per school (PRD §2.2). That removes an entire category of foundational work a multi-tenant SaaS would need (tenant resolution, connection-pooling-per-tenant, provisioning automation, migration fleet rollout) — don't reintroduce any of it. Phase 1 below is correspondingly light.
- **Bottom-up through the module dependency table** (ARCHITECTURE §5): `IdentityModule` before `AcademicStructureModule` before `SubjectModule` before `AssessmentModule`, etc. Building out of that order means mocking dependencies you'll throw away in a week.
- **Build the abstractions that are still real, even with one implementation.** `StorageAdapter` (Supabase Storage today) is cheap to build correctly now and expensive to retrofit later (ARCHITECTURE §5, §15) — don't cut it to "save time." The database connection, by contrast, needs no such abstraction — it's ordinary Prisma config, nothing to build there beyond using environment variables properly.
- **Plumbing before logic, where a pattern repeats.** The async dispatch/callback/timeout-sweep pattern is used twice (AI scheduling, ARCHITECTURE §9; payment-gateway reconciliation, ARCHITECTURE §10) — build it once, generically, the first time it's needed, and reuse it the second time rather than re-solving it.
- **Resist scope creep into PRD Non-Goals** (§1.2, §10) at every phase: no self-registration screens, no in-app "list of schools" or fleet dashboard, no Vault/KMS service, no native mobile, no automated timetable optimization beyond the CP-SAT solver. If a phase's work starts drifting into one of these, that's a signal to stop and check the PRD, not a sign the plan is incomplete.

---

## 2. Phase 0 — Repo & Infra Scaffolding

Not a PRD-numbered phase (PRD §9 starts at product Phase 1) — this is the technical groundwork that has to exist before Phase 1 work can begin.

**Tasks:**
- Monorepo scaffolding (Turborepo or Nx): `apps/api`, `apps/worker`, `apps/web`, `apps/scheduling-engine`, `packages/types`, `packages/eslint-config`, `packages/tsconfig` (ARCHITECTURE §4).
- One Prisma schema, stood up empty — first migration is just "schema exists," real tables come in per phase below.
- `docker-compose.yml`: one local Postgres DB, one local Redis, all four apps building and booting with a bare `/health` endpoint each.
- CI skeleton (GitHub Actions): lint, typecheck, unit test, build, `prisma migrate diff --exit-code` gate (ARCHITECTURE §12) — even with nothing to test yet, get the pipeline shape right early.
- Config/env loading convention, and a stub for the application-level envelope-encryption utility (ARCHITECTURE §7) — not wired to anything yet, but the pattern (encrypt/decrypt helper + where the master key comes from locally vs. in each environment) should exist before the first real secret (`PaymentGatewayConfig`) needs it in Phase 5.

**Done when:** `docker compose up` boots all four apps with working health checks, and an empty PR goes green through the full CI pipeline.

---

## 3. Phase 1 — Foundation: Auth & Core Identity

*(maps to PRD §9 Phase 1)*. Much lighter than it would be in a multi-tenant system — there's no tenant-resolution machinery to build (ARCHITECTURE §6), just ordinary application setup.

**Build in this order:**

1. **Application schema v1** — identity + academic structure only, everything else arrives in later phases as needed: `User`, `UserRole`, `Invitation`, `SchoolProfile`, `AcademicSession`, `Term`, `ClassLevel`, `ClassArm`, `Department` (PRD §3.1, §3.1a, §3.2). Migrate.
2. **Standard Prisma client**, instantiated once at process boot from environment config, injected via normal NestJS DI (ARCHITECTURE §6) — there's no factory, cache, or per-request resolution to build.
3. **Auth**: login (email + password, no tenant/slug step) → verifies credentials against the `User` table → issues JWT (`sub`, `roles`) + refresh token (rotated, Redis-blacklistable) (ARCHITECTURE §7).
4. **Invitation accept flow**: token-hash validation → set password → `User.status = active` → `Invitation.status = ACCEPTED`.
5. **CASL `AbilityFactory` + `PoliciesGuard` skeleton** — build the mechanism now even with only a handful of rules defined; every subsequent phase adds rules to it rather than inventing its own auth pattern. This is the permission boundary that actually matters here, since there's no tenant boundary to also worry about.
6. **New-school setup script** (`pnpm setup:school`, ARCHITECTURE §6.1): seeds `GradeScale`, `SchedulingConstraint`, `NotificationTemplate` defaults, creates the first `SUPER_ADMIN` `Invitation`, sends the email. One-shot, run once per deployment — no job-status table needed.

**Done when:** running the setup script against a fresh deployment gets you from an empty database to "the Proprietor received an email, accepted it, set a password, and logged in to see an empty dashboard." No tenant-isolation test is needed here — there's only ever one school's data in this database, so there's no boundary to prove.

---

## 4. Phase 2 — People

*(maps to PRD §9 Phase 2)*

1. `AdminProfile`, `StaffProfile`, `ParentProfile`, `StudentProfile`, `StudentGuardian` tables + migrations (PRD §3.1).
2. Super-Admin/Admin-driven Staff and Parent invitation flows (extends the Phase 1 `Invitation` mechanism to `invitedRole = ADMIN | STAFF | PARENT`, with Admin-appointment restricted to Super-Admin only, PRD §5, §6.1).
3. Student creation flow: atomic transaction requiring ≥1 `StudentGuardian`, with the inline-parent-invite path when the guardian doesn't already have an account (PRD §6.1 FR1.3).
4. `StaffAssignment` CRUD (class teacher, subject teacher, principal, VP, headteacher) — with `BURSAR`/`REGISTRAR` assignment restricted to Super-Admin only (PRD §5, §6.3 FR3.1). **Revised post-initial-build**: the original `/staff` UI let an Admin pick an assignment type but never actually collected `subjectId` for `SUBJECT_TEACHER`, and assigning across multiple class arms meant repeating the whole form once per arm with no way to see or edit a teacher's full set. Reworked the flow to Staff → type → Subject → Session → multi-select Class Arms (scoped to both the session and the subject's catalogued class group), backed by a new reconcile endpoint (`POST /staff-assignments/subject-teacher/sync`) that diffs the submitted arm set against existing active rows — creating/reactivating additions, soft-revoking removals in one transaction — rather than N separate create/revoke calls. A hand-added partial unique index (`WHERE "isActive" = true`, same precedent as the `SUPER_ADMIN`/`AcademicSession.isCurrent`/`ReportComment` singleton indexes) backstops it against duplicate active rows. `StaffAssignmentList` was likewise reworked from a flat per-row table into a grouped per-teacher summary (class-teacher arm, subjects with their class-arm chips, other roles), each individually revocable. Along the way, closed a second gap: the Subject picker (here and in Gradebook, below) was listing `isGroup=true` parent subjects as directly selectable — per PRD §3.3 a group is never itself taught or scored, only its `childSubjects` are — so both pickers now flatten group subjects into their children, and `ScoreEntryService` rejects a write against a group `subjectId` outright (even under Admin override) as a second line of defense.
5. Ownership transfer (PRD FR1.9) — atomic Super-Admin → another user handoff, enforced by the partial unique index on active `SUPER_ADMIN` rows.
6. Role-scoped list views: class teacher sees only her class, parent sees only her wards — the first real exercise of the CASL rules scaffolded in Phase 1.
7. Pending-invitations admin view (resend/revoke).

**Done when:** a full class of students and their parents can be onboarded (bulk import or one-by-one), every permission-scoping rule in PRD §5 is enforced, and each has a permission-boundary e2e test (one test per role, asserting what it *can't* see, not just what it can — e.g. a class teacher assigned to SSS1 Gold can never fetch a student in SSS1 Silver, no matter how the request is shaped).

---

## 5. Phase 3 — Academics

*(maps to PRD §9 Phase 3)*

1. `Subject`, `ClassSubject`, `SubjectGroupWeight`, `StudentSubjectEnrollment` (PRD §3.3).
2. Applicability rules engine: `COMPULSORY` auto-enroll, `GENERAL` opt-in, `DEPARTMENT`-gated (only SSS + matching department), plus `requiresCalculation` as data (consumed later in Phase 7).
3. `Department` + `StudentDepartment`, gated to SSS-category `ClassLevel`s only.
4. Manual `TimetableSlot` CRUD + teacher/venue double-booking conflict detection (PRD §6.6 FR6.1) — this is the class-timetable path that stays manual-or-AI-assisted throughout; the AI half comes in Phase 7.

**Done when:** Admin can define a full subject catalogue including a grouped subject (the PRD's Basic Science and Technology example — one group, four independently-scored children), assign it across class levels for a session, and students correctly auto-enroll or opt in per the type rules — verified with a test per subject type (compulsory, general, department-restricted, grouped).

---

## 6. Phase 4 — Assessment

*(maps to PRD §9 Phase 4, PRD §3.6, §3.11)*

1. `AssessmentComponent` CRUD (Admin-only) with the per-(term, classLevelCategory) structure-completeness validation (components must sum to 100 before any can open) and the three-date scheduling model (inputOpensAt/inputClosesAt/publishAt) — plus a scheduled sweep (BullMQ, same shape as the payment-gateway reconciliation / scheduling-timeout-sweep pattern in ARCHITECTURE.md §8–§10) that transitions status off those dates, with Admin override always available regardless of schedule. **Revised post-initial-build** (per user request): rescoped from per-`ClassLevel` (e.g. "JSS 1") to per-`ClassLevelCategory` class group (CRECHE/NURSERY/PRIMARY/JSS/SSS), so e.g. JSS1–JSS3 share one CA/exam structure per term instead of being configured three times over — see the hand-backfilled migration in `prisma/migrations/`.
2. `ScoreEntry` with assignment-scoped write permission (only the `SUBJECT_TEACHER` assigned to that subject+class, or Admin override), gated on the parent component's `OPEN` status. **Revised post-initial-build**: the DTO's `@Min(0)` was the only bound — no upper-bound check existed at either layer, since `maxScore` is per-`AssessmentComponent` rather than a fixed value a static `@Max()` decorator could express. `ScoreEntryService.enter` now checks `dto.score` against the loaded component's `maxScore` (regardless of override) once the component is fetched; the Gradebook UI mirrors this — blocking the minus/plus/exponent keys a native number input otherwise lets through, and showing an inline "Must be 0–N" error instead of silently posting an out-of-range value. See also the group-subject `ScoreEntry` guard noted under Phase 2's `StaffAssignment` entry above.
3. `SubjectTermResult` aggregation job — including grouped-subject weighting via `SubjectGroupWeight` (child scores roll up into the parent's grade) — triggered when a class group's components for a term all close.
4. `GradeScale`.
5. `SkillAssessmentItem` admin config (Admin/Super-Admin, per academic session), including the default-from-previous-session copy behavior on first access for a new session.
6. `ReportWindow` CRUD (Admin-only, per term+class group) gating `SkillRating` entry and `CLASS_TEACHER` `ReportComment` writes — same schedule/override shape as `AssessmentComponent`, same class-group rescoping.
7. `ReportComment` (role-scoped: subject teacher / class teacher / principal comment types).
8. `TermReportCard` generation (async PDF job, PRD FR4.8) — both a `MID_TERM` (auto-generated when the MID_TERM component closes, no gate beyond PDF completion) and `FULL_TERM` (publish gate checking subject results + skill ratings + both required comments are present) variant, per the design added to PRD §3.6. Both include the school logo/address (from `SchoolProfile`) and the generation date in the PDF header. **Revised post-initial-build, now implemented** (PRD §3.6/FR4.4a): `MID_TERM` shows only the `MID_TERM` component's score per subject (normalized to a percentage of that component's `maxScore`), each graded + given a remark via `GradeScale`, plus an overall percentage + grade line (no remark). `FULL_TERM` shows the per-subject `GradeScale` remark, a full overall average/grade/remark line, a per-component score breakdown, additive prior-term total columns (ordered by `Term.startDate`), and — for whichever term is chronologically last in the session — per-subject grade/remark/position and the overall summary computed from each subject's average across every term in the session (`SubjectTermResultService.computeAnnualSummary`), not that term's total alone. Added `SubjectTermResult.remark` and `TermReportCard.overallScore`/`overallGrade`/`overallRemark` columns; reworked `apps/worker/src/report-card/report-card.processor.ts`'s `generateMidTerm`/`generateFullTerm` and both content builders in `report-card-content.util.ts` accordingly. **Further revised once Phase 5's Attendance landed**: `FULL_TERM` now also renders the "days present / school-days-opened" attendance line (PRD §3.6/§3.7), computed via the same `computeSchoolDaysOpened`/`computeAttendancePercentage` (`packages/types`) Phase 5's attendance analytics uses, and a passport photo (`User.avatarUrl`), gender, class, session, parent/guardian name, and a QR-code authenticity signature (`TermReportCard.verificationTokenHash`, verified via the unguarded `GET /term-report-cards/verify/:token`).
9. `GET /calendar` read-aggregation endpoint (PRD §3.11) across `Term`, `AssessmentComponent`, `ReportWindow` dates, plus a basic calendar UI. **Frontend now built end-to-end** (Track B, `apps/web`): Assessment Setup (`/assessment-setup`, Admin-only manager CRUD for all five config resources), Gradebook (`/gradebook`, assignment- and window-scoped score entry), Skills & Comments (`/skills-comments`, three role-conditional sections for class-teacher ratings/comment, subject comment, principal comment), Report Cards (`/report-cards`, Admin generate/publish + role-scoped list with PDF links), and Calendar (`/calendar`, date-range list grouped by day). Along the way, fixed a `StudentService.findAllForUser` scoping gap where a STAFF user holding only a `PRINCIPAL`/`HEADTEACHER` assignment (no `CLASS_TEACHER`/`SUBJECT_TEACHER`) got an empty student list — now school-wide visibility, matching Admin, same as the real-world remit.
10. **`GET /broadsheet` — added post-initial-build** (PRD §3.6/FR4.11, PRD §5): whole-class grade grid (every student × every subject the class group is offered), Super-Admin/Principal/Headteacher-only (deliberately narrower than every other capability in this phase — plain Admin does not get it). Two independent scopes, both resolved fresh at request time rather than read from any stored per-`ClassArm` `SubjectTermResult.position`: class scope defaults to a whole `ClassLevel` (every arm under it combined, e.g. all of "JSS 1" across two arms), falling back to a single `classArmId` when narrowed down; time scope is either one `termId` or a whole `academicSessionId` ("Overall" — each subject cell becomes that student's average across whichever terms in the session actually have a result for it, same missing-term-excluded averaging `SubjectTermResultService.computeAnnualSummary` already uses, reimplemented in `apps/api/src/assessments/broadsheet.ts` since the worker's module isn't reachable from `apps/api`). Sorting (by any subject column, overall average, or overall position) and pagination (`skip`/`take`, matching `GET /students`'s `{ rows, total }` shape) are both server-side — position/ranking is always computed over the full scope before a page is sliced out of it, so a student's rank on page 2 is still correct. Frontend: `/broadsheet` (`apps/web`), gated in both the nav and the page body the same way the CASL check gates the API; scroll-to-load-more via the existing `useInfiniteScroll` hook.

**Done when:** a full term cycle runs start to finish in a test environment for at least two class groups with *different* component structures (e.g. PRIMARY with a single 20-point CA, SSS with two 10-point CAs) — components open/close/publish on schedule and via Admin override, subject teachers score within their windows, aggregation computes correct totals for both structures, every class level within a group (e.g. JSS1/JSS2/JSS3) shares that group's structure and report window, the skill list defaults correctly from a prior session (and can be built from scratch when there is none), class teachers rate skills and comment within their window, principal comments, a report card generates and publishes only once every required piece is present, the parent sees it (published-only visibility enforced), and every scheduled date involved shows up correctly on `GET /calendar`.

---

## 7. Phase 5 — Operations: Attendance, Fees, Payments (Monnify default + Paystack)

*(maps to PRD §9 Phase 5)*

1. `AttendanceSession`/`AttendanceRecord` — daily (class teacher) and per-period (subject teacher). Also build the "school-days-opened" auto-calculation design decided during Phase 4 (PRD §3.7): term date range minus weekends minus declared public holidays (a new holiday-declaration concept, e.g. `SchoolHoliday`), counted per a school-wide `DAILY`/`MORNING_AND_AFTERNOON` granularity setting — now implemented, and wired into the `FULL_TERM` `TermReportCard`'s attendance line (PRD §3.6), which Phase 4 originally shipped without.
2. `FeeStructure`, `Invoice`, `InvoiceLineItem`, `Payment`, `Receipt`, `PaymentGatewayConfig`, `DiscountRequest` — write access scoped to Super-Admin/Bursar only, per PRD §5 footnote 3 (Admin has no visibility into this domain at all); approval of `PENDING_APPROVAL` payments and `DiscountRequest`s is Super-Admin only, not Bursar (PRD §6.7). This is the first place the Phase 0 envelope-encryption stub gets wired to a real secret (`PaymentGatewayConfig.apiKey`/`secretKey`, now one encrypted row per provider rather than a single singleton row).
3. **`PaymentGatewayAdapter` interface first, provider adapters second** (PRD §6.7, ARCHITECTURE §5, §10) — same "build the abstraction even with one implementation" principle as `StorageAdapter` (§1): define `initTransaction`/`verifyTransaction`/`verifyWebhookSignature` once, then implement `MonnifyAdapter` (bound by default, `PAYMENT_GATEWAY_PROVIDER=MONNIFY`) and `PaystackAdapter` behind it. `FeesModule` and the webhook controller call only the interface.
   - Checkout initiation (reference unique per invoice — no tenant-routing concern to design around, ARCHITECTURE §10).
   - Webhook handler: verify signature via the active adapter → resolve `Payment`/`Invoice` directly by reference → idempotency check on `gatewayTransactionReference` → update (recording which `gatewayProvider` handled it) → generate `Receipt` → notify.
   - Reconciliation polling job for missed webhooks, per the `Payment`'s own recorded `gatewayProvider` — **this is the first occurrence of the generic async dispatch/timeout-sweep pattern** that Phase 7's AI scheduling reuses; build it as a reusable pattern here rather than a one-off, so Phase 7 doesn't redesign it.
4. **Manual bank-transfer payment workflow** (PRD FR7.3a/FR7.3b, ARCHITECTURE §10.2): Bursar submits a `Payment(method=BANK_TRANSFER_MANUAL)` with a proof-of-payment file via `StorageAdapter` → `PENDING_APPROVAL`; Super-Admin reviews (approve → `SUCCESSFUL`, generate `Receipt`, notify Bursar + parent; reject → `REJECTED` with reason, notify Bursar). This is a synchronous approval action, not a queued job — it doesn't touch the `payment-reconciliation` queue from step 3.
5. **Discount request workflow** (PRD FR7.8, ARCHITECTURE §10.3): Bursar raises a `DiscountRequest` against an invoice (termly by construction, since an invoice is already per-term); Super-Admin approves (adds a `DISCOUNT` `InvoiceLineItem`, recomputes outstanding balance) or rejects (notify Bursar with reason).
6. **Receipts and payment history** (PRD FR7.4/FR7.4a, ARCHITECTURE §10.4): unify receipt generation across all three paths above (gateway webhook/poll, manual approval, cash entry); build the parent-facing invoice/receipt history view and the Bursar/Super-Admin school-wide payment ledger view.

**Done when:** a parent can complete a real (sandbox) transaction end-to-end on the **default (Monnify)** gateway and see the invoice flip to paid with a receipt generated — verified via **both** paths independently: the normal webhook path, and a forced-webhook-failure test proving the polling fallback alone resolves the payment (PRD FR7.6) — **and** the same checkout flow works against the Paystack sandbox after only flipping `PAYMENT_GATEWAY_PROVIDER` (no code change), proving the adapter abstraction actually holds. Additionally: a Bursar-submitted manual bank-transfer payment reaches a parent's confirmed invoice only after Super-Admin approval (never before, and never approvable by Bursar or Admin); a rejected submission notifies the Bursar with a reason and never touches the invoice; a Bursar-raised discount request changes the invoice's outstanding balance only after Super-Admin approval, correctly scoped to that one term's invoice; and a parent, Bursar, and Super-Admin can each pull up historical payment records at the appropriate scope (own wards vs. school-wide).

---

## 8. Phase 6 — Notifications & Polish

*(maps to PRD §9 Phase 6)*

1. WebSocket gateway + Redis adapter, rooms scoped `user:{userId}` (ARCHITECTURE §8) — no tenant prefix needed.
2. `NotificationTemplate` seeded from default templates shipped in the codebase at Phase 1's setup step, `Notification`, `NotificationPreference`.
3. `EmailLog` + Resend delivery webhook handling (bounce/complaint tracking flags the guardian record for follow-up, PRD FR8.4).
4. `AuditLog` wired as a cross-cutting interceptor (ARCHITECTURE §5) across every write path added in Phases 1–5 — this is retroactive coverage, not new business logic, so budget time for it rather than treating it as an afterthought.
5. Performance pass: N+1 query audit across the modules built so far, P95 latency check against the PRD §7 NFR target (<300ms reads).

**All five items above are now built, plus the follow-up work items 1–2 deliberately deferred** (wiring `notify()` into real write paths, and the two notification types — `PASSWORD_RESET`/`INVOICE_OVERDUE` — that had no trigger yet):

- **Items 1–2**: `NotificationsGateway` (Socket.IO + `@socket.io/redis-adapter`), `NotificationService.notify()` (template render, per-channel `NotificationPreference` check, in-app row + live socket push + `EmailLog`/email-dispatch enqueue), `GET/PATCH /notifications*`, `GET/PATCH /notification-preferences`, `GET/PATCH /notification-templates` (Admin-customizable copy). `GET /notifications` is backend-paginated (`{ data, total }`, same contract as `InvoiceService`/`DiscountRequestService.findAllForUser`) with `unreadOnly`/`type` filters.
- **Item 3**: `EmailLog` + a real `email-dispatch` BullMQ queue (apps/worker's `EmailProcessor`, Resend send with retry) + a Resend/Svix-signed webhook (`POST /email-webhooks/resend`) updating `EmailLog.status` and flagging `ParentProfile.emailBounced` on bounce/complaint.
- **Item 4**: `AuditLog` + a single global `AuditInterceptor` (`@Audited(entityType, model?)` decorator) — annotated on every sensitive write path across score entries, fees, and role/assignment changes. Deliberate security exception: `PaymentGatewayConfig` never gets a before-fetch (would expose encrypted `apiKey`/`secretKey` into a second table).
- **Item 5**: found and fixed one real N+1 (`InvoiceService.generate` — was one `findUnique` per active student, now one batch `findMany`); two more (a worker-side term-result aggregation, a mid-term report-card enqueue loop) were identified but left alone — background jobs, not on the NFR's read-latency path, and the fix would touch load-bearing grade-computation logic for uncertain benefit. P95 measured against 150 synthetic students: all sampled read endpoints 7–30× under the 300ms target.
- **Follow-up (notify() actually wired in)**: `DiscountRequestService.approve/reject` → Bursar; `PaymentService.recordCash`/`resolveGatewayOutcome` → paying guardian(s); `PaymentService.approveManualBankTransfer/rejectManualBankTransfer` → Bursar; `TermReportCardService.publish` (FULL_TERM) → every guardian. `INVITATION_RECEIVED` deliberately stays unwired (would duplicate the existing direct `MailerService` invite email).
- **Follow-up (password reset, PRD FR1.8, built from scratch)**: `POST /auth/forgot-password`/`POST /auth/reset-password`, Redis-based single-use hashed token (same shape as refresh tokens, not a DB table — `Invitation`'s longer-lived table pattern fit an onboarding workflow, not this), 1-hour TTL, revokes every outstanding refresh token on success (reusing `AuthService.revokeAllRefreshTokens`, built during Phase 1 for exactly this).
- **Follow-up (invoice-overdue sweep, PRD FR8.4's `INVOICE_OVERDUE` type, built from scratch)**: hourly BullMQ sweep in apps/worker flips a past-due `Invoice` to `OVERDUE` and notifies every guardian. `NotificationService` lives in apps/api, unreachable from apps/worker across the process boundary — resolved with a documented-duplicate `WorkerNotificationService` (same "duplicated per-process" precedent as `PaymentService.resolveGatewayOutcome`), minus the live socket push (no gateway in the worker process — an in-app row still appears on next poll/reconnect).
- **Frontend** (apps/web): a topbar notification bell (unread badge live via the socket, recent-list dropdown) plus a `/notifications` page (backend-paginated scroll-to-load-more, `unreadOnly`/`type` filters) and `/forgot-password`/`/reset-password` pages. The WebSocket handshake needs the raw access JWT client-side, which the httpOnly-cookie proxy architecture doesn't expose — bridged with one narrow `/api/socket-token` route that hands the current token to already-authenticated client code, nothing else.

**Done when:** every PRD FR that says "trigger notification" actually fires both in-app and email, unread counts update live over the socket, critical notification types (report card published, invoice overdue, password reset) can't be muted via `NotificationPreference`, and `AuditLog` has an entry for every sensitive write path added so far (score entries, fee records, role/assignment changes). All true now, with the one named exception (`INVITATION_RECEIVED` intentionally not wired).

---

## 9. Phase 7 — AI Scheduling

*(maps to PRD §9 Phase 7)* — deliberately last, since it depends on `Subject.requiresCalculation` (Phase 3), `StaffAssignment` (Phase 2), and `AssessmentComponent` (Phase 4) all already existing.

1. **Async plumbing first, solver logic second**: `ScheduleGenerationRequest` (PRD §3.8) — including its `parameters` jsonb column for run-specific overrides (mid-term exam duration/subject-count, duty team size) — + BullMQ dispatch job + callback endpoint with per-request `callbackToken` + timeout sweep (ARCHITECTURE §9). If Phase 5's payment-gateway reconciliation already established this generic async pattern, this step is mostly reuse, not new design.
2. `scheduling-engine` service (Python/FastAPI + OR-Tools CP-SAT): class timetable model — subject/day/period assignment honoring `SchedulingConstraint` rows, `requiresCalculation` morning-placement and spread rules.
3. Extend to exam timetable generation (calculation-subject-first, minimum gap between calculation exams). For `MID_TERM`-type components specifically, honor the run's `maxSubjectsPerDay` and calculation/non-calculation `...DurationMinutes` parameters (PRD FR6.3a) when placing slots and deriving each `ExamSchedule.startTime`/`endTime`.
4. Extend to invigilation assignment (staff eligibility exclusion: `BURSAR`/`PRINCIPAL`/`VICE_PRINCIPAL`, load balancing, no double-booking, and — hard-excluded for `JSS`/`SSS` runs, best-effort for `CRECHE`/`NURSERY`/`PRIMARY` runs — never assigning a subject's own teacher to invigilate that subject's exam, PRD FR6.4).
5. Extend to weekly duty roster generation (PRD FR6.11/FR6.12): one `DutyAssignment` row per staff member per week across a `Term`, admin-specified `teachersPerWeek`, fair rotation via `MIN_WEEKS_BETWEEN_REPEAT_DUTY`, generated separately per `JSS_SSS`/`CRECHE_NURSERY_PRIMARY` group same as the other two scopes.
6. Approval workflow UI: `PENDING_REVIEW → APPROVED`, with the split approval rule from PRD §5 (class timetables: Admin or Super-Admin; exam timetables, invigilation rosters, and weekly duty rosters: Admin only, footnote 4).
7. Whole-school timetable overview UI (PRD FR6.10, §5 footnote 6): by-day (Mon–Fri) and by-class grouped views, scoped server-side per requester — Super-Admin/Admin/Registrar unscoped, Principal to `JSS`/`SSS`, Headteacher to `CRECHE`/`NURSERY`/`PRIMARY`. Visual reference: `docs/school-system-design.html` "Timetable" panel.

**Done when:** a realistic SSS1–3 constraint set (real subject list, real staff roster, real `SchedulingConstraint` values) produces a usable draft class timetable, exam schedule (both `EXAM` and, with admin-supplied max-subjects-per-day/duration parameters, `MID_TERM`), invigilation roster, and weekly duty roster requiring only minor manual edits — verified end-to-end through the full async pipeline, including one deliberately-simulated timeout case that exercises FR6.9. A generated JSS/SSS invigilation roster never assigns a subject's own teacher to that subject's exam (verified as a hard constraint, not just a preference); the `CRECHE`/`NURSERY`/`PRIMARY` case keeps it best-effort. The whole-school overview correctly scopes Principal/Headteacher to their class-level range and lets Super-Admin/Admin/Registrar switch between by-day and by-class grouping.

**Status: items 1–5 and 7 built, verified by code inspection, plus the previously-missing `SchedulingConstraint` UI now built; item 6 is implemented and, per an explicit product decision, matches the shipped code rather than the original PRD wording; the timeout-sweep half of the "Done when" verification bar now has real test coverage, the realistic end-to-end solve run still doesn't.**

- **Items 1–5, 7 — implemented as designed, no material gaps.** `ScheduleGenerationRequest` + BullMQ dispatch (`apps/worker/src/scheduling-solve-dispatch/`) + timeout sweep (`apps/worker/src/scheduling-timeout-sweep/`, 5-minute repeatable job, configurable `SCHEDULING_TIMEOUT_MINUTES`, default 10) + callback controller (`apps/api/src/exam-scheduling/scheduling-callback.controller.ts`, `timingSafeEqual` token compare) match ARCHITECTURE.md §9's diagram closely. `apps/scheduling-engine` is a real, non-stub OR-Tools CP-SAT service across all four scopes (`class_timetable.py`, `exam_timetable.py`, `invigilation.py`, `weekly_duty.py` — ~890 lines total): class timetable honors `requiresCalculation` morning-placement/spread and teacher no-double-booking; exam timetable enforces calculation-subject-first placement, `MIN_GAP_BETWEEN_CALCULATION_EXAMS_DAYS`, and per-day `maxSubjectsPerDay` capacity (both `EXAM` and `MID_TERM` duration/count parameters honored); invigilation excludes `BURSAR`/`PRINCIPAL`/`VICE_PRINCIPAL`, load-balances, and — for `JSS`/`SSS` — never even creates the decision variable for a subject's own teacher (`invigilation.py:82-83`, a true hard constraint, not a penalized one), while `CRECHE`/`NURSERY`/`PRIMARY` uses a soft penalty term as designed; weekly duty models `MIN_WEEKS_BETWEEN_REPEAT_DUTY` as a dominant (but not absolute) objective term, matching the PRD's "unless the eligible pool is too small to avoid it" language, generated independently per `ClassLevelCategoryGroup`. The whole-school overview (`AllClassesTimetableView`/`AllClassesExamTimetableView`, `apps/web`) implements By-day/By-class toggles with server-side scoping (`TimetableSlotService.resolveCategoryGroupScopedClassArmIds`) — Principal to JSS/SSS, Headteacher to CRECHE/NURSERY/PRIMARY, Super-Admin/Admin/Registrar unscoped — matching FR6.10 exactly.
- **Item 6 — resolved by product decision, no code change needed**: the original PRD wording specified a split approval rule — class timetables approvable by Admin *or* Super-Admin; exam timetables, invigilation rosters, and weekly duty rosters approvable by Admin *only*. The shipped code never implemented that split: `ScheduleGenerationRequestService.assertCanApproveBatch` (`apps/api/src/exam-scheduling/schedule-generation-request.ts`) and the frontend's approvals-queue gating (`apps/web/src/components/organisms/scheduling-approvals-queue.tsx`, wired via `canAct={isSuperAdmin}` in `apps/web/src/app/planner/page.tsx`) have always restricted all four scopes to **Super-Admin only**. An explicit product decision (2026-08-14) settled this the other way — Super-Admin-only approval across all four scopes is now the intended behavior, not a bug — so PRD.md §5 (permission matrix, footnote 4, FR6.5/FR6.7/FR6.12) was updated to match the shipped code instead of the reverse. Admin/Principal/Headteacher retain generate/edit rights on drafts; only final approval narrows to Super-Admin.
- **Verification gap, partially closed**: `scheduling-timeout-sweep.processor.spec.ts` now covers the FR6.9 timeout path (marks stuck `QUEUED`/`SOLVING` requests `TIMED_OUT`, respects the configurable `SCHEDULING_TIMEOUT_MINUTES` threshold with a fake-timers-pinned cutoff assertion, defaults to 10 minutes when unset, and keeps sweeping the rest of the batch when one requester's `notify()` call fails — same "isolated failure doesn't stop the sweep" shape as `invoice-overdue-sweep.processor.spec.ts`). Still no test file for `scheduling-solve-dispatch.processor.ts` or any of the four Python solver modules (`apps/scheduling-engine` declares `pytest` as a dev dependency but ships zero test files), and no fixture/e2e test runs a realistic SSS1–3 dataset through the pipeline — that half of the "Done when" bar remains open, folded into the Cross-Cutting security/CI hardening pass (§10).
- **Frontend gap, closed**: `SchedulingConstraintManager` (`apps/web/src/components/organisms/scheduling-constraint-manager.tsx`) is wired in as a "Constraints" tab, gated to Admin/Super-Admin only (matching the backend's `manage SchedulingConstraint` CASL grant — Principal/Headteacher/Registrar don't get it, unlike the other tabs). Grouped by scope, then by class-level group, with add/edit/delete and a typed value editor (boolean/number/text/comma-separated list) over the untyped jsonb column; the group picker is hidden for `INVIGILATION` since the worker only ever reads that scope's constraints as global (the JSS/SSS hard-exclude split is derived from the exam's own component category, not a per-group row). Building this surfaced and fixed a real backend gap along the way: `CreateSchedulingConstraintDto`/`UpdateSchedulingConstraintDto` (`apps/api/src/exam-scheduling/dto/scheduling-constraint.dto.ts`) were missing the `classLevelCategoryGroup` field entirely — since `ValidationPipe`'s `forbidNonWhitelisted` rejects any field not in the DTO, the API could only ever create/edit the global (`null`-group) rows, never the per-group override rows the worker actually queries for `CLASS_TIMETABLE`/`EXAM_TIMETABLE`/`WEEKLY_DUTY` (`scheduling-solve-dispatch.processor.ts`). Fixed by adding the field as an optional `@IsEnum(ClassLevelCategoryGroup)` to the create DTO (inherited by the `PartialType` update DTO). Verified end-to-end via a headless-browser smoke test (Admin add/edit/delete a group-scoped constraint, row persists correctly through all three operations against the real API/DB; Principal and Registrar accounts confirmed not to see the tab at all).
- **Nav rename + opened visibility (2026-08-14)**: the page (formerly `/scheduling`, "Scheduling") is now `/planner`, labeled "Planner", per an explicit product decision. The sidebar link's `visible` predicate (previously Admin/Registrar/Principal/Headteacher only) was removed entirely so every authenticated user sees it. `TimetableSlot`/`ExamSchedule` needed no backend change — both already defaulted to APPROVED-only with no CASL gate of their own, and Parent/Principal/Headteacher scoping already existed (`resolveParentScopedClassArmIds`/`resolveCategoryGroupScopedClassArmIds` in both services), matching PRD FR6.7's "class timetable and exam schedule are visible to students/parents scoped to their own class" rule. `InvigilationAssignment`/`DutyAssignment` did need a backend fix, since FR6.7 draws a different line for those two — visible to staff/Registrar/Super-Admin/Admin but **never** to students or parents (no scoping-down, an outright exclusion): both controllers' `findAll` now call a new `assertNotStudentOrParent` guard (a pure Parent/Student account, holding neither `STAFF` nor `ADMIN`/`SUPER_ADMIN`, gets a 403) before the existing APPROVED-only/manage-check logic runs. The frontend mirrors this: the Invigilation/Weekly Duty tabs are hidden (trigger *and* content, not just the manage-only cards inside them) for the same non-staff account shape (`canSeeStaffRosters` in `apps/web/src/app/planner/page.tsx`). The "Generate & Approve" tab is separately hidden entirely (not just its manage-only cards) for anyone who isn't Admin/Super-Admin/Registrar/Principal/Headteacher, matching the existing Admin-only gate already used for the "Constraints" tab.

---

## 10. Cross-Cutting Work (Not Owned by One Phase)

All three items below were written at Phase 0 as forward-looking commitments to revisit "as each phase introduces a new failure surface." With Phases 1–7 now functionally complete, none of the three has actually been revisited since Phase 0 — each still reflects the original bare-minimum scaffolding. This is real, not aspirational, debt at this point (every phase that was supposed to trigger a follow-up has now passed), so this section is rewritten below as current-state-plus-plan rather than a forward promise.

### 10.1 Observability

**Status: still exactly the Phase 0 baseline.** Both `apps/api/src/health/health.controller.ts` and `apps/worker/src/health/health.controller.ts` are unchanged from their original stub — each still literally comments "Phase 0: no dependency checks yet" / "Phase 0: no BullMQ queues exist yet" and returns a bare `{ status: "ok" }`, despite Postgres, Redis, Resend, BullMQ queues, and the payment gateway all having been live dependencies since Phases 1–5. No structured logging (pino or otherwise — API/worker both run on Nest's default logger) and no error tracking (no Sentry or equivalent dependency anywhere in the repo) have been added, so ARCHITECTURE §13's structured-logging and error-tracking items are both still undone, not just unconfigured.

**Plan**: before this school goes live, do the deferred extension in one pass rather than piecemeal:

1. `/health` on both API and worker: check DB (`SELECT 1`), Redis ping, and — API only — Resend reachability and the active `PaymentGatewayConfig` provider's reachability, per ARCHITECTURE §13.
2. Add BullMQ queue-depth/age metrics per queue (`scheduling-solve-dispatch`, `scheduling-timeout-sweep`, `payment-reconciliation`, `email-dispatch`, `report-card-generation`, `assessment-schedule-sweep`, `invoice-overdue-sweep`) and the `Payment.status = PENDING_APPROVAL` count metric called out in ARCHITECTURE §13 — none of this exists yet.
3. Pick and wire a structured logger (pino, per ARCHITECTURE §13) with per-request correlation IDs, and an error tracker (Sentry or equivalent) — currently zero visibility into production errors beyond whatever's in stdout.

### 10.2 Security hardening passes

**Status: neither of the two planned passes has happened.** No pass ran after Phase 2, and none has run since (this repo is not yet live, so the pre-launch pass is still pending too — but the gap is larger than "not yet due," since concrete pieces the *first* pass was supposed to cover are simply missing from the code):

- **PRD §7 explicitly requires** "rate-limiting on auth and invitation-accept endpoints (NestJS Throttler)" — `@nestjs/throttler` is not a dependency anywhere in `apps/api/package.json`, and no rate-limiting exists on `/auth/login`, `/auth/forgot-password`, or `/invitations/accept`. This is a documented requirement that was never built, not a nice-to-have.
- **No permission-boundary e2e tests exist.** `apps/api/src/identity/ability.factory.spec.ts` unit-tests the CASL `AbilityFactory` in isolation, but there is no `.e2e-spec.ts` anywhere in the repo (`apps/api/src` has 44 `.spec.ts` files, all unit-level) that fuzzes real HTTP requests per role the way Phase 2's own "Done when" criteria call for ("one test per role, asserting what it *can't* see... no matter how the request is shaped"). This gap is inherited by every phase after Phase 2 that added new role-scoped endpoints, not just Phase 2's original surface.
- **No dependency audit or secret-rotation drill** has been run or documented anywhere in the repo.

**Plan**: run the originally-scoped "after Phase 2" pass now (it's overdue, not skippable) — add `@nestjs/throttler` to auth/invitation-accept endpoints, and add a real permission-boundary e2e suite (one test file per role, hitting actual controllers, across every module built so far: identity, academic-structure, assessments, fees, exam-scheduling). Fold in the AI-scheduling verification gap noted in §9 above (a timeout-sweep test, a realistic end-to-end SSS1–3 solve test) while this pass is open, since it's the same class of "add real test coverage" work. Run the pre-launch pass (secret-rotation drill, dependency audit) once a go-live date is set, not before — no need to front-load it speculatively.

### 10.3 CI/CD maturity

**Status: still the Phase 0 pipeline, now stale rather than just minimal.** `.github/workflows/ci.yml`'s `prisma migrate diff` step still carries its original Phase 0 comment verbatim — *"Phase 0 has no migrations yet; once prisma/migrations exists this step should instead diff --from-migrations against the schema and --exit-code, failing the build on drift"* — but `prisma/migrations/` now has 34 real migrations, and the step still only runs `--from-empty --to-schema-datamodel ... --script > /tmp/expected.sql` with no `--exit-code` gate and nothing checked against the output. In its current form the step can't actually fail a build on schema drift; it just silently generates a file nobody reads. Lint/typecheck/test/build all run and gate correctly for both the Node and Python halves of CI.

**Plan**: implement the migration-diff gate the Phase 0 comment already specifies — diff `prisma/migrations` against the current `schema.prisma` with `--exit-code`, failing CI on drift between the migration history and the schema file. This is a small, well-scoped fix (the comment already describes exactly what to do); no design work needed, just execution. Fleet-wide CI/CD templating (ARCHITECTURE §12) stays out of scope until a second school deployment is actually being stood up, per the original plan — that part of the original text still holds.

---

## 11. Suggested Parallelization (if more than one builder)

- **Track A (backend core)**: Phase 1 foundation → Phase 3/4 academics & assessment. This track owns auth/identity and can't be parallelized much within itself — it's inherently sequential early on.
- **Track B (frontend)**: Next.js shell + design system can start as soon as `packages/types` has stub DTOs for whatever Phase 1 exposes, and stays roughly one phase behind whichever backend phase is active — never blocking, but never far ahead either, since it needs real contracts to build against.
- **Track C (specialists, self-contained)**: the payment gateway integration (Phase 5) and the AI scheduling engine (Phase 7) don't block and aren't blocked by the academics/assessment work in Phases 3–4 — a specialist (or a second engineer) can pick either up in parallel once Phase 1's async-plumbing pattern exists, rather than waiting for Phase 5/7 to come up in strict sequence. Within Phase 5 itself, the `PaystackAdapter` is a natural second-engineer task once the `PaymentGatewayAdapter` interface and `MonnifyAdapter` exist — it doesn't touch `FeesModule`'s core invoice/receipt logic.

---

## 12. What Not to Build Early

Resist building these ahead of when the PRD/ARCHITECTURE actually call for them — each is either an explicit Non-Goal or a deferred decision, and building it early is wasted work against a moving target:

- Any tenant-resolution machinery — TenantContextMiddleware-style connection switching, per-tenant client caching, a tenant claim in the JWT. None of it applies here; this is a single-tenant application (ARCHITECTURE §6).
- A Vault/KMS service — application-level envelope encryption is the decided default; don't stand up Vault infrastructure preemptively.
- A fleet-management/multi-school ops dashboard — not needed until the team is actually operating enough separate school deployments that manually tracking them becomes painful (PRD §10, ARCHITECTURE §15), and even then it's explicitly outside this product.
- Anything from PRD §1.2 Non-Goals or §10 Open Questions framed as "deferred" — multi-currency billing, native mobile, fully autonomous (unreviewed) schedule publishing, cross-school single sign-on.
